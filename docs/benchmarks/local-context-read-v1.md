연결된 `Local Mac Context (Private)`의 `local-context_read_project_excerpt` 도구를 실제로 호출하여 `fixture/source-marker.txt`를 읽어줘.

도구가 반환한 `path`, `content`, `sha256`, `digest_basis`만 그대로 답해줘. 읽기가 실패하면 실패 이유만 알려줘. 값을 추측하거나 다른 출처에서 찾지 말고, 문서 생성·수정이나 다른 작업은 하지 마.
